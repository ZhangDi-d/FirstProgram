# coding:utf-8

"""
isinstance

type

vars 变字典

dirs

hasattr hasattr(person,'name')

getattr getattr(person,'name')

setattr setattr(person,'name','lisi')

any any([1,0,''])

all
"""
list = ['a', 'b', 'c']

for item, index in enumerate(list):
    print(item, index)
